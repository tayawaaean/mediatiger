{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "functions": {
    "api/**/*.ts": {
      "runtime": "@vercel/node"
    }
  },
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node",
      "config": {
        "tsconfig": "tsconfig.api.json"
      }
    }
  ]
}